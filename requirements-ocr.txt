# Optional: image translation module (OCR + render back onto the image)
#   pip install -r requirements-ocr.txt
# Default engine: RapidOCR with PP-OCRv5 models on onnxruntime - no
# paddlepaddle required.
#
# PP-OCRv6 upgrade (better accuracy, heavier): additionally install
#   pip install paddleocr paddlepaddle
# and the app will automatically prefer PaddleOCR with PP-OCRv6.
rapidocr>=3.0
onnxruntime>=1.17
opencv-python-headless>=4.8
